[project]
name = "m5paper-dash-astro-server"
version = "0.1.0"
description = "Server scripts for M5Paper astronomy dashboard"
requires-python = ">=3.10"
dependencies = [
  "ephem>=4.1.5",
  "httpx>=0.28.0",
  "python-dotenv>=1.0.0",
]

